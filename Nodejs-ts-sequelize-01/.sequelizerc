    const path = require('path');

    module.exports = {
      'migrations-path': path.resolve(__dirname, 'dist/migrations/'),
      // Other configurations like 'config', 'models-path', 'seeders-path' if needed
    };
